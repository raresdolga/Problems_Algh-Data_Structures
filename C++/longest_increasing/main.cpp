#include <bits/stdc++.h>

using namespace std;
void set_M(int arr[], int sz) {
    for(int i = 0; i < sz; i++ ){

    }
}
int main()
{
    int n, arr[200005], int nr, j;
    for(int i = 0; i < n; i++){
        cin >> nr;
        for(j = 0; j < nr; j++)
            cin >> arr[j];
        set_M(arr,nr);
    }
           cout << "Hello world!" << endl;
    return 0;
}
